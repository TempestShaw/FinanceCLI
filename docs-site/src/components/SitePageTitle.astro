---
import DefaultPageTitle from '@astrojs/starlight/components/PageTitle.astro';
const home = Astro.url.pathname.replace(/\/$/, '') === import.meta.env.BASE_URL.replace(/\/$/, '');
---
{!home && <DefaultPageTitle />}
